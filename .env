# Database Credentials (These must match our docker-compose.yml)
DB_HOST=localhost
DB_PORT=5437
DB_USER=cafe_admin
DB_PASSWORD=supersecretpassword
DB_NAME=cafe_pos

# We will use this in Phase 3!
JWT_SECRET=my_ultimate_secret_key_for_cafe_tokens